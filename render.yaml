services:
  - type: web
    name: student-mlops-api
    env: python
    plan: free
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn src.serve.app:app --host 0.0.0.0 --port $PORT"
    runtime: "python-3.10"